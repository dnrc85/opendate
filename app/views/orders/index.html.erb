<p id="notice"><%= notice %></p>

<h2>Outstanding Orders</h2>

<%= link_to 'New Order', new_order_path, class: "btn btn-primary" %><%= link_to 'All outstanding parts', parts_all_path, class: "btn btn-primary" %>

<table class = "table">
  <thead>
    <tr>
      <th>Order Number</th>
      <th>Customer</th>
      <th>Region</th>
      <th>Printer</th>
      <th>Quantity</th>
      <th>Order Date</th>
      <th>Due Date</th>
      <th>Parts Remaining</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= link_to order.order_number, order %></td>
        <td><%= link_to order.customer, order %></td>
        <td><%= order.region %></td>
        <td><%= order.printer %></td>
        <td><%= order.quantity %></td>
        <td><%= order.order_date %></td>
        <td><%= order.due_date %></td>
        <td><%= order.parts.count %></td>
        <td><%= link_to 'Mark complete', order, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>